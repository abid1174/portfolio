---
import BrandIcon from './BrandIcon.astro';
import AppearanceControls from '../react/AppearanceControls';
import { isActiveRoute, navigation } from '../../data/navigation';
import { site } from '../../data/site';

const pathname = Astro.url.pathname;
---

<aside class="sidebar" id="site-sidebar">
  <div class="sidebar-scroll">
    <!-- Profile -->
    <div class="profile">
      <a class="profile-id" href="/">
        <span class="avatar" aria-hidden="true">{site.avatarInitials}</span>
        <span class="profile-text">
          <span class="profile-name">{site.name}</span>
          <span class="profile-role">{site.role}</span>
        </span>
      </a>

      <p class="profile-bio">{site.intro}</p>

      <ul class="profile-social">
        {
          site.social.map((link) => (
            <li>
              <a
                href={link.href}
                rel="me noopener noreferrer"
                target="_blank"
                title={link.label}
                aria-label={link.label}
              >
                <BrandIcon name={link.label as 'GitHub' | 'LinkedIn'} size={18} />
              </a>
            </li>
          ))
        }
      </ul>
    </div>

    <!-- Primary navigation -->
    <nav class="nav" aria-label="Primary">
      <ul>
        {
          navigation.map((item) => {
            const Icon = item.icon;
            const active = !item.action && isActiveRoute(item.href, pathname);
            return (
              <li class={item.pillar ? `pillar-${item.pillar}` : undefined}>
                {item.action === 'search' ? (
                  <button type="button" class="nav-link is-action" data-open-search>
                    <Icon size={16} strokeWidth={2} aria-hidden="true" />
                    <span class="nav-label">{item.label}</span>
                    <kbd class="nav-kbd" aria-hidden="true">
                      ⌘K
                    </kbd>
                  </button>
                ) : (
                  <a
                    class="nav-link"
                    href={item.href}
                    aria-current={active ? 'page' : undefined}
                  >
                    <Icon size={16} strokeWidth={2} aria-hidden="true" />
                    <span class="nav-label">{item.label}</span>
                  </a>
                )}
              </li>
            );
          })
        }
      </ul>
    </nav>

    <!-- Personal statement -->
    <div class="sidebar-foot">
      <p class="statement">{site.learningStatement}</p>
      <div class="sidebar-theme">
        <AppearanceControls client:idle />
      </div>
    </div>
  </div>
</aside>

<style>
  .sidebar {
    display: flex;
    flex-direction: column;
    width: var(--sidebar-width);
    height: 100dvh;
    position: sticky;
    top: 0;
    flex-shrink: 0;
    border-right: 1px solid var(--border-subtle);
    background: var(--bg-primary);
  }

  .sidebar-scroll {
    display: flex;
    flex-direction: column;
    height: 100%;
    overflow-y: auto;
    overscroll-behavior: contain;
  }

  /* --- Profile --- */

  .profile {
    padding: 1.15rem 1rem 1.1rem;
    border-bottom: 1px solid var(--border-subtle);
  }

  .profile-id {
    display: flex;
    align-items: center;
    gap: 0.65rem;
    text-decoration: none;
    border-radius: var(--radius-md);
  }

  .avatar {
    display: grid;
    place-items: center;
    width: 2.25rem;
    height: 2.25rem;
    flex-shrink: 0;
    border-radius: var(--radius-md);
    border: 1px solid var(--border-default);
    background: var(--surface);
    font-family: var(--font-mono);
    font-size: 0.8125rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    color: var(--text-primary);
  }

  .profile-text {
    display: flex;
    flex-direction: column;
    min-width: 0;
  }

  .profile-name {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--text-primary);
    line-height: 1.3;
  }

  .profile-role {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text-muted);
  }

  .profile-bio {
    margin: 0.85rem 0 0;
    font-size: 0.75rem;
    line-height: 1.55;
    color: var(--text-muted);
  }

  .profile-social {
    display: flex;
    gap: 0.15rem;
    margin: 0.85rem 0 0 -0.4rem;
    padding: 0;
    list-style: none;
  }

  /* Icon-only. A square hit area with no chrome at rest; the surface
     and the accent colour arrive together on hover. */
  .profile-social a {
    display: grid;
    place-items: center;
    width: 2rem;
    height: 2rem;
    border-radius: var(--radius-md);
    color: var(--text-muted);
    text-decoration: none;
    transition:
      color var(--dur-fast) var(--ease-out),
      background-color var(--dur-fast) var(--ease-out);
  }
  .profile-social a:hover {
    color: var(--pillar);
    background: var(--surface-inset);
  }
  .profile-social :global(svg) {
    display: block;
  }

  /* --- Navigation --- */

  .nav {
    padding: 0.9rem 0.6rem;
    flex: 1;
  }

  .nav ul {
    display: grid;
    gap: 2px;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .nav-link {
    position: relative;
    display: flex;
    align-items: center;
    gap: 0.7rem;
    width: 100%;
    padding: 0.62rem 0.65rem;
    border: 0;
    border-radius: var(--radius-md);
    background: transparent;
    color: var(--text-secondary);
    font-family: inherit;
    font-size: var(--text-base);
    font-weight: 500;
    text-align: left;
    text-decoration: none;
    cursor: pointer;
    transition:
      background-color var(--dur-fast) var(--ease-out),
      color var(--dur-fast) var(--ease-out);
  }

  .nav-link :global(svg) {
    flex-shrink: 0;
    color: var(--text-muted);
    transition: color var(--dur-fast) var(--ease-out);
  }

  .nav-link:hover {
    background: var(--surface-hover);
    color: var(--text-primary);
  }
  .nav-link:hover :global(svg) {
    color: var(--pillar);
  }

  .nav-link[aria-current='page'] {
    background: var(--surface-inset);
    color: var(--text-primary);
    font-weight: 600;
  }
  .nav-link[aria-current='page'] :global(svg) {
    color: var(--pillar);
  }

  /* Active rail on the left edge of the item. */
  .nav-link[aria-current='page']::before {
    content: '';
    position: absolute;
    left: -0.6rem;
    top: 50%;
    transform: translateY(-50%);
    width: 2px;
    height: 1.15rem;
    border-radius: 0 2px 2px 0;
    background: var(--pillar);
  }

  .nav-label {
    flex: 1;
    min-width: 0;
  }

  .nav-kbd {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    color: var(--text-muted);
    border: 1px solid var(--border-subtle);
    border-radius: 4px;
    padding: 0.05rem 0.25rem;
  }

  /* --- Foot --- */

  .sidebar-foot {
    padding: 0.9rem 1rem 1rem;
    border-top: 1px solid var(--border-subtle);
  }

  .statement {
    margin: 0 0 0.85rem;
    font-size: 0.6875rem;
    line-height: 1.6;
    color: var(--text-muted);
  }

  @media (max-width: 64rem) {
    .sidebar {
      position: fixed;
      inset: 0 auto 0 0;
      z-index: var(--z-drawer);
      width: min(19rem, 84vw);
      transform: translateX(-100%);
      transition: transform var(--dur-base) var(--ease-out);
      box-shadow: var(--shadow-lg);
    }

    :global(body[data-drawer='open']) .sidebar {
      transform: none;
    }
  }
</style>
