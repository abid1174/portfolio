---
/**
 * Wraps every MDX table so wide tables scroll inside their own box
 * instead of pushing the page into horizontal scroll.
 */
---

<div class="table-scroll">
  <table><slot /></table>
</div>
